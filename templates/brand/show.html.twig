{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('brand_index') }}" class="btn btn-primary active" role="button>Back to list</a>

    <a href="{{ path('brand_edit', {'id': brand.id}) }}" class="btn btn-primary active" role="button">Edit</a>

    {{ include('brand/_delete_form.html.twig') }}
{% endblock %}
